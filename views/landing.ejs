<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>KYC Portal - Login or Signup</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #059669 0%, #10b981 25%, #34d399 75%, #6ee7b7 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .main-container {
      max-width: 1400px;
      margin: 0 auto;
      display: grid;
      grid-template-columns: 1fr 500px 1fr;
      gap: 30px;
      align-items: start;
    }

    /* Center Column - Login/Signup */
    .center-panel {
      background: white;
      border-radius: 16px;
      padding: 40px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.1);
      backdrop-filter: blur(10px);
    }

    .center-panel h1 {
      text-align: center;
      margin-bottom: 30px;
      color: #1e293b;
      font-size: 2rem;
    }

    /* Tab Navigation */
    .tab-nav {
      display: flex;
      margin-bottom: 30px;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .tab-button {
      flex: 1;
      padding: 15px 20px;
      background: #f8fafc;
      border: none;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
      transition: all 0.3s ease;
      color: #64748b;
    }

    .tab-button.active {
      background: #2563eb;
      color: white;
    }

    .tab-button:hover:not(.active) {
      background: #e2e8f0;
    }

    /* Form Sections */
    .form-section {
      display: none;
      animation: fadeIn 0.3s ease;
    }

    .form-section.active {
      display: block;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .form-section form {
      display: flex;
      flex-direction: column;
    }

    label {
      margin: 15px 0 5px;
      font-weight: 600;
      color: #374151;
    }

    input, select {
      padding: 12px;
      margin-bottom: 15px;
      border-radius: 8px;
      border: 2px solid #e5e7eb;
      font-size: 16px;
      transition: border-color 0.3s ease;
    }

    input:focus, select:focus {
      outline: none;
      border-color: #2563eb;
    }

    button {
      padding: 12px 20px;
      background: linear-gradient(135deg, #2563eb, #1e40af);
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      font-weight: 600;
      transition: transform 0.2s ease;
      margin-top: 10px;
    }

    button:hover {
      transform: translateY(-2px);
    }

    .error {
      color: #dc2626;
      font-size: 0.9em;
      margin-bottom: 15px;
    }

    /* Side Panels - Information Banners */
    .side-panel {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .banner {
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
      border-left: 6px solid;
      transition: transform 0.3s ease;
    }

    .banner:hover {
      transform: translateY(-5px);
    }

    .banner h3 {
      margin-bottom: 15px;
      font-size: 1.2rem;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .banner ul {
      list-style: none;
      padding: 0;
    }

    .banner ul li {
      margin-bottom: 10px;
      padding-left: 20px;
      position: relative;
      color: #4b5563;
      line-height: 1.5;
    }

    .banner ul li::before {
      content: "→";
      position: absolute;
      left: 0;
      color: inherit;
      font-weight: bold;
    }

    /* Banner Color Themes */
    .banner.customer {
      border-left-color: #3b82f6;
    }

    .banner.customer h3 {
      color: #1e40af;
    }

    .banner.organization {
      border-left-color: #10b981;
    }

    .banner.organization h3 {
      color: #047857;
    }

    .banner.regulator {
      border-left-color: #f59e0b;
    }

    .banner.regulator h3 {
      color: #92400e;
    }

    .banner.admin {
      border-left-color: #ef4444;
    }

    .banner.admin h3 {
      color: #b91c1c;
    }

    /* Responsive Design */
    @media (max-width: 1200px) {
      .main-container {
        grid-template-columns: 1fr;
        gap: 20px;
      }
      
      .side-panel {
        flex-direction: row;
        flex-wrap: wrap;
      }
      
      .banner {
        flex: 1;
        min-width: 280px;
      }
    }

    @media (max-width: 768px) {
      .center-panel {
        padding: 20px;
      }
      
      .side-panel {
        flex-direction: column;
      }
      
      .banner {
        min-width: auto;
      }
    }
  </style>
</head>
<body>
  <div class="main-container">
    <!-- Left Side Panel -->
    <div class="side-panel">
      <div class="banner customer">
        <h3>👤 Customer Guide</h3>
        <ul>
          <li>Sign up and upload your KYC documents securely</li>
          <li>Monitor which organizations request access</li>
          <li>Approve or reject access requests with control</li>
          <li>Grant specific documents with custom access levels</li>
        </ul>
      </div>

      <div class="banner organization">
        <h3>🏢 Organization Guide</h3>
        <ul>
          <li>Register and log in as an organization</li>
          <li>Search for customers by name or ID</li>
          <li>Send access requests specifying the purpose</li>
          <li>View approved documents once granted</li>
        </ul>
      </div>
    </div>

    <!-- Center Panel - Login/Signup -->
    <div class="center-panel">
      <h1>KYC Portal</h1>
      
      <div class="tab-nav">
        <button class="tab-button active" onclick="showTab('login')">Login</button>
        <button class="tab-button" onclick="showTab('signup')">Sign Up</button>
      </div>

      <!-- Login Form -->
      <div id="login" class="form-section active">
        <form action="/login" method="POST">
          <label>Login As:</label>
          <select name="role" required>
            <option value="customer">Customer</option>
            <option value="organization">Organization</option>
            <option value="admin">Admin</option>
            <option value="regulator">Regulator</option>
          </select>

          <label>Login ID:</label>
          <input type="text" name="Login_ID" required>

          <label>Password:</label>
          <input type="password" name="password" required>

          <button type="submit">Login</button>
        </form>
      </div>

      <!-- Signup Form -->
      <div id="signup" class="form-section">
        <form action="/register" method="POST" onsubmit="return validatePasswords()">
          <label>Sign Up As:</label>
          <select name="role" required>
            <option value="customer">Customer</option>
            <option value="organization">Organization</option>
          </select>

          <label>Login ID:</label>
          <input type="text" name="Login_ID" required>

          <label>Password:</label>
          <input type="password" name="password" id="password" required>

          <label>Confirm Password:</label>
          <input type="password" name="confirm_password" id="confirmPassword" required>

          <div id="error-message" class="error"></div>

          <button type="submit">Sign Up</button>
        </form>
      </div>
    </div>

    <!-- Right Side Panel -->
    <div class="side-panel">
      <div class="banner regulator">
        <h3>🛡️ Regulator Guide</h3>
        <ul>
          <li>Log in as a regulator with special privileges</li>
          <li>Monitor compliance across all platform users</li>
          <li>Review document requirements by organization</li>
          <li>Audit access logs and approval history</li>
        </ul>
      </div>

      <div class="banner admin">
        <h3>⚙️ Admin Guide</h3>
        <ul>
          <li>Manage users and their platform roles</li>
          <li>Monitor platform activity and usage</li>
          <li>Configure document types and organization sectors</li>
          <li>Oversee platform health and system settings</li>
        </ul>
      </div>
    </div>
  </div>

  <script>
    function showTab(tabName) {
      // Hide all form sections
      const sections = document.querySelectorAll('.form-section');
      sections.forEach(section => section.classList.remove('active'));
      
      // Remove active class from all buttons
      const buttons = document.querySelectorAll('.tab-button');
      buttons.forEach(button => button.classList.remove('active'));
      
      // Show selected section and activate button
      document.getElementById(tabName).classList.add('active');
      event.target.classList.add('active');
    }

    function validatePasswords() {
      const password = document.getElementById("password").value;
      const confirmPassword = document.getElementById("confirmPassword").value;
      const errorMessage = document.getElementById("error-message");

      if (password !== confirmPassword) {
        errorMessage.textContent = "Passwords do not match.";
        return false;
      }

      errorMessage.textContent = "";
      return true;
    }
  </script>
</body>
</html>